[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[requires]
python_version = "3.10"

[packages]
langchain = ">=0.3,<0.4"
langchain-openai = ">=0.2,<0.3"
langsmith = ">=0.1,<0.3"
gradio = ">=6.17.3,<7.0"
python-dotenv = ">=1.0,<2.0"

[dev-packages]
pytest = ">=8.0,<9.0"
pytest-mock = ">=3.12,<4.0"
